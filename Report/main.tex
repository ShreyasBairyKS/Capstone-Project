\documentclass[12pt,a4paper]{report}

% ---------------------------------------------------------------------
% Packages
% ---------------------------------------------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{url}
\usepackage{hyperref}
\usepackage{setspace}
\usepackage{geometry}
\usepackage{titlesec}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{xurl}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{tikz}
\usetikzlibrary{calc,backgrounds}
\usepackage{listings}
\usepackage[style=ieee]{biblatex}
\addbibresource{biblio.bib}
\lstset{
  language=Python,
  basicstyle=\small\ttfamily,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{red},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=2,
  showstringspaces=false
}

% Make code listings appear in a neat boxed style
\lstset{
  frame=single,
  framerule=0.6pt,
  framesep=6pt,
  rulecolor=\color{black!60},
  backgroundcolor=\color{gray!6},
  captionpos=b,
  xleftmargin=0pt,
  xrightmargin=0pt
}

\geometry{
  a4paper,
  margin=1in
}

\onehalfspacing
\raggedbottom
\setlength{\parindent}{0.5in}
\setlength{\parskip}{0pt}
\setlength{\headheight}{15pt}
\setlength{\emergencystretch}{2em}
\justifying

\hypersetup{
  colorlinks=true,
  citecolor=blue,
  linkcolor=blue,
  urlcolor=blue
}

\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}

\DeclareFieldFormat{doi}{%
  \mkbibacro{DOI}\addcolon\space\url{https://doi.org/#1}}

\newcommand{\projecttitle}{VisionFood QAI: AI-Powered Quality Inspection for Food and Beverage Manufacturing}
\newcommand{\shorttitle}{VisionFood QAI}
\newcommand{\studentone}{Shreyas Bairy K S}
\newcommand{\usnone}{1BM23CD058}
\newcommand{\studenttwo}{Sushanth S}
\newcommand{\usntwo}{1BM23CD063}
\newcommand{\studentthree}{Ullas N}
\newcommand{\usnthree}{1BM23CD067}
\newcommand{\guidename}{Nayana N Kumar}
\newcommand{\department}{Department of Computer Science and Engineering (Data Science)}
\newcommand{\academicyear}{2025--2026}

% Border applied consistently to every shipped page.
\newcommand{\DrawPageBorder}{%
  \begin{tikzpicture}[remember picture,overlay]
    \draw [on background layer,rounded corners=7pt, line width=1.2pt,
           color=black!90, double distance=1.5pt, double=black!50]
      ($(current page.north west)+(0.4cm,-0.4cm)$)
      rectangle
      ($(current page.south east)+(-0.4cm,0.4cm)$);
\end{tikzpicture}%
}
\AddToHook{shipout/foreground}{\DrawPageBorder}

\titleformat{\chapter}[display]
  {\normalfont\bfseries}
  {\fontsize{14}{18}\selectfont\chaptername\ \thechapter}
  {10pt}
  {\centering\fontsize{14}{18}\selectfont}

\titleformat{\section}
  {\normalfont\fontsize{12}{14}\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\fontsize{12}{14}\bfseries\itshape}{\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\normalfont\fontsize{12}{14}\bfseries\itshape}{\thesubsubsection}{1em}{}

\titlespacing*{\chapter}{0pt}{0pt}{18pt}
\titlespacing*{\section}{0pt}{12pt}{6pt}
\titlespacing*{\subsection}{0pt}{10pt}{4pt}

\fancypagestyle{frontmatterstyle}{
  \fancyhf{}
  \fancyfoot[C]{\roman{page}}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

\fancypagestyle{chapterstyle}{
  \fancyhf{}
  \fancyhead[L]{\textbf{\shorttitle}}
  \fancyfoot[L]{\small Dept. of CSE (Data Science), BMSCE}
  \fancyfoot[C]{\small \academicyear}
  \fancyfoot[R]{\thepage}
  \renewcommand{\headrulewidth}{1pt}
  \renewcommand{\footrulewidth}{1pt}
}

\begin{document}
\pagenumbering{gobble}

% ---------------------------------------------------------------------
% Title Page
% ---------------------------------------------------------------------
\begin{titlepage}
\begin{center}
  \textsc{\Large \textbf{\textcolor{blue}{B.M.S. COLLEGE OF ENGINEERING}}}\\[0.2cm]
  \textsc{\large \textbf{(Autonomous Institute, Affiliated to VTU)}}\\[0.2cm]
  \textsc{\large \textbf{Bull Temple Road, Basavangudi, Bengaluru - 560019}}\\[0.5cm]

  \includegraphics[scale=0.72]{logo.png}\\[0.25cm]
  \textit{A Thesis on}\\[0.25cm]
  {\large \textbf{\textcolor{red}{\projecttitle}}}\\[0.35cm]
  \textit{in partial fulfillment of the requirements for the award of the degree}\\[0.25cm]

  \textbf{BACHELOR OF ENGINEERING}\\[0.15cm]
  \textit{in}\\[0.2cm]
  \textbf{Computer Science \& Engineering (Data Science)}\\[0.3cm]

  \textit{by}\\[0.2cm]
  \textbf{\studentone\ (\usnone)}\\[0.15cm]
  \textbf{\studenttwo\ (\usntwo)}\\[0.15cm]
  \textbf{\studentthree\ (\usnthree)}\\[0.65cm]

  \textbf{Under the Guidance of}\\[0.25cm]
  \textbf{\guidename}\\[0.15cm]
  Assistant Professor\\[0.15cm]
  \textbf{\department}\\
  \textbf{B.M.S. College of Engineering}\\[0.35cm]

  \textbf{Academic Year \academicyear}
\end{center}
\end{titlepage}

% ---------------------------------------------------------------------
% Front Matter
% ---------------------------------------------------------------------
\pagenumbering{roman}
\pagestyle{frontmatterstyle}

\newpage
\addcontentsline{toc}{chapter}{Declaration}
\begin{center}
  \textsc{\Large \textbf{\textcolor{blue}{B.M.S. COLLEGE OF ENGINEERING}}}\\[0.2cm]
  \textsc{\large \textbf{(Autonomous Institute, Affiliated to VTU)}}\\[0.2cm]
  \textsc{\large \textbf{Bull Temple Road, Basavangudi, Bengaluru - 560019}}\\[0.5cm]
  \includegraphics[width=0.2\textwidth]{logo.png}\\[0.2cm]
  \textbf{\Large \underline{DECLARATION}}
\end{center}

\noindent We hereby declare that the project thesis entitled \textbf{\textit{\projecttitle}} is a bonafide work carried out by us under the guidance of \textbf{\guidename}, \department, B.M.S. College of Engineering, Bengaluru, in partial fulfillment of the requirements for the degree of Bachelor of Engineering in Computer Science \& Engineering (Data Science) of Visvesvaraya Technological University, Belagavi.

\noindent We further declare that, to the best of our knowledge and belief, this thesis has not been submitted either in part or in full to any other university or college for the award of any degree.

\vspace{0.5cm}
\begin{center}
  \textbf{\underline{Candidate Details}}
\end{center}

\begin{center}
\begin{tabularx}{\textwidth}{|c|Y|c|c|}
  \hline
  \textbf{Sl. No.} & \textbf{Student Name} & \textbf{USN} & \textbf{Student Signature} \\
  \hline
  1 & \studentone & \usnone & \\
  \hline
  2 & \studenttwo & \usntwo & \\
  \hline
  3 & \studentthree & \usnthree & \\
  \hline
\end{tabularx}
\end{center}

\vfill
\noindent Date: \rule{4cm}{0.4pt} \hfill Place: Bengaluru

\newpage
\addcontentsline{toc}{chapter}{Certificate}
\begin{center}
  \textsc{\Large \textbf{\textcolor{blue}{B.M.S. COLLEGE OF ENGINEERING}}}\\[0.1cm]
  \textsc{\large \textbf{(Autonomous Institute, Affiliated to VTU)}}\\[0.1cm]
  \textsc{\large \textbf{Bull Temple Road, Basavangudi, Bengaluru - 560019}}\\[0.4cm]
  \includegraphics[scale=0.4]{logo.png}\\[0.1cm]
  \textbf{\department}\\[0.15cm]
  \textbf{\Large \underline{CERTIFICATE}}\\[0.2cm]
\end{center}

\noindent This is to certify that the project thesis entitled \textbf{\textit{\projecttitle}} is a bonafide work carried out by the following students for the Capstone Project under the Department of Computer Science and Engineering (Data Science). It is certified that the work reported herein has been carried out under the guidance of \textbf{\guidename} and satisfies the academic requirements prescribed for the Bachelor of Engineering degree in Computer Science \& Engineering (Data Science).

\vspace{0.5cm}
\begin{center}
  \textbf{\underline{Candidate Details}}
\end{center}

\begin{center}
\begin{tabularx}{\textwidth}{|c|Y|c|}
  \hline
  \textbf{Sl. No.} & \textbf{Student Name} & \textbf{USN} \\
  \hline
  1 & \studentone & \usnone \\
  \hline
  2 & \studenttwo & \usntwo \\
  \hline
  3 & \studentthree & \usnthree \\
  \hline
\end{tabularx}
\end{center}

\vspace{1.2cm}
\begin{center}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} l c r}
\textbf{\guidename} & \textbf{Dr. Shambhavi B R} & \textbf{Dr. Bheemsha Arya} \\
Assistant Professor & Professor \& HOD & Principal \\
\end{tabular*}
\end{center}

\vspace{0.6cm}
\begin{center}
  \textbf{Examiners}\\[0.2cm]
\end{center}
\noindent \textbf{Name of Examiner} \hfill \textbf{Signature of Examiner}\\[0.2cm]
\noindent \textbf{1.} \hfill \rule{5cm}{0.4pt}\\[0.2cm]
\noindent \textbf{2.} \hfill \rule{5cm}{0.4pt}

\newpage
\addcontentsline{toc}{chapter}{Acknowledgments}
\begin{center}
  \textbf{\Large Acknowledgments}
\end{center}

\noindent The satisfaction that accompanies the completion of this Capstone Project would be incomplete without acknowledging the support and guidance that made the work possible. We express our sincere gratitude to \textbf{Dr. B. S. Ragini Narayan}, Chairperson, Donor Trustee, Member Secretary and Chairperson, BMSET; \textbf{Dr. P. Dayananda Pai}, Member Life Trustee, BMSET; and \textbf{Dr. Bheemsha Arya}, Principal, B.M.S. College of Engineering, for providing the academic environment and infrastructure required for this project.

\noindent We are grateful to \textbf{Dr. Shambhavi B R}, Professor and Head of the Department, Computer Science and Engineering (Data Science), and to the project coordinators for their constant support and encouragement. We express our heartfelt thanks to our guide, \textbf{\guidename}, for her valuable guidance, timely feedback, and encouragement throughout the project.

\noindent We also thank all faculty members of the Department of Computer Science and Engineering (Data Science), our classmates, and everyone who contributed directly or indirectly to the successful progress of this work.

\vspace{1.5cm}
\begin{flushright}
\studentone\ (\usnone)\\
\studenttwo\ (\usntwo)\\
\studentthree\ (\usnthree)
\end{flushright}

\newpage
\chapter*{\centering Abstract}
\addcontentsline{toc}{chapter}{Abstract}

Today's food and beverage manufacturing operates at scales and speeds that challenge manual quality control. Inspectors working on high-speed production lines struggle to maintain consistency and catch small defects-yet every missed flaw can reach consumers, damaging brand reputation and trust. This thesis addresses this critical need by presenting \shorttitle, an AI-powered visual inspection system designed specifically for packaged food and beverage products.

Rather than relying on rigid rules or single-defect detection, our approach combines real-world practicality with advanced machine learning. The system learns to identify four common visual problems: improper filling, packaging damage, label misalignment, and surface contamination. Behind the scenes, modern deep learning models-YOLOv8 for detecting defects and Mobilenet V3 for confirming them, work together to ensure accurate, explainable decisions. To handle uncertainty, we incorporate Monte Carlo Dropout, which quantifies when the model is unsure and flags those cases for human review rather than making risky automatic decisions.

Unlike traditional inspection systems that simply mark products as pass or reject, \shorttitle includes a REMEDY engine that understands product damage realistically. A label that's slightly misaligned might be fixable through relabeling; underfilled bottles can go to a refill station; serious contamination must be quarantined. The system uses SKU-specific product profiles-defining custom inspection rules for bottle\_250ml, can\_330ml, and pouch\_100g formats so the same software pipeline can intelligently inspect very different package types without being told which is which each time.

On the operator side, the system runs FastAPI backend services, logs every inspection for auditing, streams real-time updates, generates shift reports as PDFs, and displays a React dashboard showing live metrics, defect patterns, severity breakdowns, and inspection history. The design prioritizes edge-first deployment using ONNX Runtime, keeping latency below 80 milliseconds per frame so production doesn't slow down.

The literature review identifies significant gaps: beverages are underrepresented in AI quality research, most systems focus narrowly on one defect or package type, and few systems integrate detection with explainability and remediation. We address these gaps through our unified architecture. The project requires a minimum of 400 real annotated images for training, with an ideal target of 1000 or more for production robustness. Our performance targets include reliable defect localization, high sensitivity to real flaws, uncertainty-aware verdicting, and edge latency meeting real manufacturing constraints.

\vspace{1em}
\noindent\textbf{Keywords:} Food quality inspection; beverage inspection; deep learning; YOLOv8; explainable AI; edge deployment; operator dashboard.

\newpage
\addcontentsline{toc}{chapter}{Table of Contents}
\tableofcontents

\newpage
\addcontentsline{toc}{chapter}{List of Tables}
\listoftables

\newpage
\addcontentsline{toc}{chapter}{List of Figures}
\listoffigures

\newpage
\addcontentsline{toc}{chapter}{List of Abbreviations}
\chapter*{\centering List of Abbreviations}
\begin{tabularx}{\textwidth}{|P{3cm}|Y|}
  \hline
  \textbf{Abbreviation} & \textbf{Expansion} \\
  \hline
  AI & Artificial Intelligence \\
  \hline
  AIoT & Artificial Intelligence of Things \\
  \hline
  CNN & Convolutional Neural Network \\
  \hline
  DCAI & Data-Centric Artificial Intelligence \\
  \hline
  F1 & Harmonic mean of precision and recall \\
  \hline
  HACCP & Hazard Analysis and Critical Control Points \\
  \hline
  IoT & Internet of Things \\
  \hline
  mAP & Mean Average Precision \\
  \hline
  PLC & Programmable Logic Controller \\
  \hline
  REMEDY & Recommendation, Escalation, Mitigation, Evidence, Diagnosis, and Yield triage engine \\
  \hline
  RFID & Radio Frequency Identification \\
  \hline
  SKU & Stock Keeping Unit \\
  \hline
  XAI & Explainable Artificial Intelligence \\
  \hline
  YOLO & You Only Look Once \\
  \hline
\end{tabularx}

\newpage
\pagenumbering{arabic}
\setcounter{page}{1}
\pagestyle{chapterstyle}

% ---------------------------------------------------------------------
% Chapter 1
% ---------------------------------------------------------------------
\chapter{Introduction}

\section{Background}
\label{sec:background}

Food and beverage manufacturing is built on a foundation of quality, safety, and trust. When a customer opens a package expecting a perfect product, they should find exactly that, not a leaking container, dented packaging, misaligned label, or visible contamination. Behind every flawless product on a store shelf lies rigorous quality control. A single oversight can cascade: defective packages leak during transport, compromise food safety, create liability, damage brand reputation, and undermine consumer confidence.

Beverage production lines operate at breathtaking speeds-sometimes hundreds of bottles or cans per minute. In these environments, spotting defects becomes almost superhuman: underfilled bottles, broken seals, dented cans, tilted caps, missing labels, and stains blur past human inspectors. Manual inspection, despite its long history, has fundamental limitations. Inspectors tire during long shifts, their attention drifts, and they miss small defects hidden by reflections on shiny plastic or glass. Yet in many modern facilities, manual visual inspection is still the backbone of quality assurance.

Machine vision has been part of manufacturing for decades. Traditional systems using cameras and rule-based logic can measure fill levels, verify label positions, and detect gross defects. Farhangi et al. showed that careful engineering can achieve 95.6\% accuracy on transparent bottles using classical computer vision techniques \cite{farhangi2024machine}. However, these rule-based approaches have a hidden cost: they're brittle. Change the bottle design, adjust the label, update the cap color, or switch packaging suppliers and the entire system needs recalibration. For products that evolve regularly, the maintenance burden becomes prohibitive.

Deep learning has opened new possibilities. Convolutional Neural Networks learn visual patterns directly from data, making them more adaptable than hand coded rules. Systems based on ResNet, DenseNet, and similar architectures can now detect food packaging defects with high accuracy \cite{banus2021thermoforming,subramanian2025resnet}. But classification simply marking a product as good or bad isn't enough for operators. They need to know \emph{where} the problem is and \emph{why} it was flagged. YOLO-based object detection answers this need, localizing defects so operators see the exact location of the problem \cite{kavitha2024deeplearning}.

Despite this progress, a fragmentation persists in research and industry. Beverage inspection forms only a small fraction of ML-based food quality research \cite{liakos2025review}. Most systems are narrow: inspect only liquid level, or only cap sealing, or only one product family. Real factories need systems that handle \emph{multiple} defect types, work across \emph{different} package formats, provide auditable explanations, and offer practical remediation guidance not just pass/fail verdicts.

\shorttitle\ emerges from this context. It's designed as a complete inspection workflow, not just a model. A camera captures the product, AI localizes candidate defects, uncertainty estimation flags low-confidence decisions, and a REMEDY engine routes products intelligently: some items can be relabeled, others refilled, serious safety issues quarantined. The system uses SKU-specific profiles, so the same software pipeline handles transparent bottles, rigid cans, flexible pouches, and rigid boxes without requiring the model to classify product type in every frame. Rather than a notebook experiment, we've built a production-oriented stack: FastAPI backend, real-time WebSocket updates, database auditing, PDF reports, and a React dashboard that operators actually use.

\section{Objectives}
\label{sec:objectives}

The primary goal of this project is to design a clear, understandable, and practical deep learning architecture for visual quality inspection in food and beverage manufacturing not just a model, but a deployable system.

Our specific objectives are:
\begin{enumerate}
  \item To understand and synthesize recent research on deep learning classification, YOLO-based defect detection, IoT-driven quality monitoring, autoencoder-based anomaly detection, weakly supervised approaches, and adaptive learning for industrial inspection.
  \item To identify specific limitations in today's food and beverage inspection systems: insufficient beverage research, narrow single-defect focus, lack of localization plus explanation integration, weak remediation support, and scarce public datasets.
  \item To design an RGB-camera-based inspection pipeline that can detect and locate four concrete visual problems: improper filling, packaging damage, label misalignment, and surface contamination.
  \item To combine YOLOv8 detection with an MobileNet V3 classification stage, so that defect locations are supported by a secondary verification and confidence scoring.
  \item To implement SKU-level product profiles, allowing configurable inspection of transparent bottles, rigid cans, flexible wrappers, and rigid boxes without forcing the model to re-identify product type constantly.
  \item To integrate Monte Carlo Dropout for honest uncertainty estimation and build a REMEDY severity and routing engine so outputs are transparent, auditable, and actionable for plant operators.
  \item To define concrete success metrics: precision, recall, F1-score, mAP@0.5, false rejection rates, sub-80-millisecond latency per frame, and complete traceability from image to decision.
\end{enumerate}

\section{Problem Statement}
\label{sec:problem-statement}

Manual quality inspection cannot scale with modern manufacturing demands. Human inspectors become fatigued, inconsistent, and miss defects on fast conveyor lines. Yet most industrial systems respond with only pass/reject verdicts offering no path to recovery for fixable problems. The broader research landscape shows that beverage inspection is underexplored, most systems handle only one or two defect types, and few provide the localization, explainability, and operator traceability that plants actually require.

The main challenge we address is the lack of  unified, understandable, and operator-ready deep learning architecture for multi-defect visual inspection across different food and beverage package formats. Such a system must:

\begin{itemize}
  \item Pinpoint defects in images, not just classify them,
  \item Provide evidence and reasoning that operators can review and trust,
  \item Support different product formats with configurable rules, not one-size-fits-all logic,
  \item Offer guidance on remediation (relabel, refill, quarantine, review), not just rejection,
  \item Generate auditable records so quality decisions are traceable and defensible.
\end{itemize}

Additionally, the system must respect manufacturing constraints: low-latency edge deployment, reliability on resource limited hardware, and resilience to production-line variability.

Our scope focuses on visual defects visible in RGB images: improper filling, packaging deformation, label placement errors, and surface contamination. We deliberately exclude (for now) chemical spoilage, microbial contamination, and non-visual attributes, though the architecture can integrate additional sensors later.

\section{Project-Specific Design Scope}
\label{sec:project-specific-scope}

This is not an academic model comparison; it's a production-oriented workflow. Every product receives a verdict, evidence, and a permanent record. Table~\ref{tab:project-scope} captures our scope.

\begin{center}
\refstepcounter{table}
\addcontentsline{lot}{table}{\protect\numberline{\thetable}{Project-specific scope of \shorttitle}}
\textbf{Table~\thetable: Project-specific scope of \shorttitle}
\label{tab:project-scope}

\vspace{0.2cm}
\begin{tabularx}{\textwidth}{|P{3.2cm}|Y|}
\hline
\textbf{Component} & \textbf{Project-specific decision} \\
\hline
Target products & Packaged food and beverage formats, beginning with bottle\_250ml, can\_330ml, and pouch\_100g SKU profiles. \\
\hline
Defect taxonomy & Improper filling, packaging damage, label misalignment, and surface contamination. \\
\hline
Inference design & YOLOv8 detector for localization, MobileNet V3 classifier for confirmation, and ONNX Runtime for edge inference. \\
\hline
Decision logic & PASS, FAIL, FAIL with review flag, ESCALATE, and REVIEW based on confidence and uncertainty bands. \\
\hline
REMEDY output & Severity grades S1--S4 and routing actions such as relabel, refill, reject, quarantine, or human inspection. \\
\hline
Operator layer & FastAPI backend, inspection database, WebSocket event stream, React dashboard, KPI cards, Pareto chart, severity chart, and PDF shift report. \\
\hline
Dataset requirement & Minimum 400 real annotated images, ideally 1000 or more, with YOLO-format bounding boxes and a 70:15:15 train-validation-test split. \\
\hline
\end{tabularx}
\end{center}

\section{Summary}
\label{sec:intro-summary}

This chapter has outlined why automated visual quality inspection matters in food and beverage manufacturing, and why today's systems often fall short. Manual inspection cannot scale; rule-based systems are inflexible; and most AI systems are too narrow. We've presented \shorttitle\ as a practical answer: a complete pipeline combining deep learning, configurable SKU profiles, uncertainty quantification, REMEDY-based routing, and operator-facing dashboards. 

The next chapter surveys the research landscape, synthesizes what works, identifies persistent gaps, and explains how \shorttitle\ addresses them.

% ---------------------------------------------------------------------
% Chapter 2
% ---------------------------------------------------------------------
\chapter{Literature Review}
\label{ch:literature-review}

\section{Review Method and Source Selection}
\label{sec:review-method}

The literature survey was prepared from the research papers stored in the project repository and from the existing project paper draft. The reviewed material covers food quality control, beverage-line inspection, general industrial defect detection, IoT-based food monitoring, packaging inspection, weakly supervised defect detection, and adaptive industrial visual inspection. The review treats sixteen papers as the core technical literature and includes two additional supporting works: the systematic food-quality-control review by Liakos et al. \cite{liakos2025review} and the partially relevant multiple-instance-learning surface-defect paper by Xu et al. \cite{xu2024mil}. This allows the survey to remain aligned with the project abstract while still accounting for every paper consulted from the repository.

The papers are grouped thematically instead of chronologically because the project requires a synthesis of methods and gaps rather than a year-wise listing. Each paper was examined against a project module: detection, classification, uncertainty, product-profile configuration, remediation, dashboard traceability, or dataset preparation. The main themes are CNN and transfer learning methods, YOLO and object detection methods, beverage and packaging machine-vision systems, IoT and sensor fusion, and specialized methods such as autoencoders, incremental learning, and weak supervision.

\section{CNN and Transfer Learning Approaches}
\label{sec:cnn-transfer}

CNN-based methods are widely used for image classification in quality inspection because they learn hierarchical visual features directly from images. Banus et al. proposed a deep learning system for quality control of thermoforming food packages and compared ResNet18, ResNet50, VGG19, and DenseNet161 with both pretrained and non-pretrained configurations \cite{banus2021thermoforming}. Their production-line validation is especially important because the system was tested not only in laboratory settings but also in a real scenario. The best pretrained DenseNet161 configuration achieved very low false positive and false negative rates, demonstrating that deep learning can satisfy strict food-packaging inspection requirements when imaging conditions and defect definitions are carefully controlled.

Subramanian et al. explored optimized ResNet50 for real-time defect detection in manufacturing \cite{subramanian2025resnet}. The work supports the value of transfer learning, especially where industrial datasets are smaller than general image-recognition datasets. ResNet50 is attractive because residual connections help preserve gradient flow in deeper networks and improve representation learning for cracks, dents, and misalignments. However, the model size and computation requirements can become challenging for low-cost edge devices unless pruning, quantization, or lightweight backbones are applied.

Chatchapol et al. presented a beverage-specific CNN and image-processing system for liquid filling quality control \cite{chatchapol2024liquid}. The system inspected liquid level and lid sealing while also considering conveyor speed control. It reported 98.89\% accuracy for liquid-level detection and 100\% accuracy for lid sealing. This paper is directly relevant because it addresses beverage production, but its inspection scope is limited mainly to fill and cap quality rather than the broader set of packaging, label, and contamination defects targeted in \shorttitle.

Sanjay proposed a CNN-based food oil quality and usage detection system \cite{sanjay2024foodoil}. The work shows that image-based AI can support food quality assessment beyond package geometry. However, the domain is narrow and does not address industrial production-line localization, traceability, or multi-defect packaging inspection. Xiong et al. proposed a computer-vision-based automated quality-control artifact for food packaging using a data-centric AI approach \cite{xiong2024artifact}. Their framework is significant because it evaluates multiple quality factors, including visual and informational aspects, and emphasizes that improving data quality can be more valuable than only changing the model architecture.

Overall, CNN and transfer learning studies show that classification accuracy can be high when the product class and defect taxonomy are well-defined. Their limitation is that classification alone often cannot localize the defective region. For \shorttitle, this directly motivates the two-stage design: YOLOv8 first proposes defect regions, and MobileNet V3 then checks the cropped region with more class-specific focus. This is more useful to an operator than a pass/fail label because the dashboard can show the defective area, the confirmation class, and the uncertainty attached to the decision.

\section{YOLO and Object Detection Methods}
\label{sec:yolo-detection}

Object detection is central to industrial inspection because defects must be localized before products can be rejected, reworked, or investigated. YOLO models are especially popular because they perform detection in a single forward pass, making them suitable for real-time production settings.

Kavitha and Mamatha surveyed automated product defect detection using image processing and YOLOv5 for sorting and quality assurance \cite{kavitha2024automated}. The paper highlights the transition from manual and rule-based inspection to machine-learning-based detection in manufacturing. A related paper by Kavitha et al. implemented YOLO-based product defect detection for sustainable smart manufacturing and integrated hardware actuation using Arduino-based sorting \cite{kavitha2024deeplearning}. This demonstrates an end-to-end inspection-and-sorting concept, but low-cost actuation may not scale directly to high-speed industrial food and beverage lines.

Nguyen et al. proposed an industrial system based on machine vision and YOLOv8, with data multi-threading and PLC integration for real-time operation \cite{nguyen2025industrial}. The work is important because it moves beyond isolated model training and demonstrates how image acquisition, inference, and actuator communication interact in an industrial pipeline. The reported accuracy above 90\% across defect types shows the feasibility of YOLOv8 in production-style environments, although the paper is not specific to food or beverages.

Liu et al. developed an improved YOLOv5 model for automatic food packaging defect detection \cite{liu2025foodpkg}. The model integrates CBAM attention, feature pyramid and path aggregation networks, and adaptive spatial feature fusion. It was trained on 7400 food-packaging images and reported accuracy 0.96, recall 0.94, and F1-score 0.94. This is the closest prior work to the proposed project because it detects multiple packaging defects in a food-related setting. However, it does not provide operator-facing explainability, remediation guidance, or a broader product-profile mechanism for different package formats.

Gediya discussed a smart manufacturing framework that combines AI image recognition, IoT devices, and edge computing for real-time quality control \cite{gediya2024smart}. Although the paper is more architectural than experimental, it is relevant to \shorttitle\ because it frames quality inspection as part of a larger Industry 4.0 ecosystem. The insight is that detection models must be integrated with data flow, edge processing, production feedback, and continuous improvement mechanisms.

YOLO-based literature confirms that real-time localization is feasible, but most systems stop at detection output. \shorttitle\ treats the detector as only the first decision layer. If YOLOv8 finds no defect, the product follows a fast PASS path. If it finds a defect, the cropped patch is passed to the classifier and uncertainty module before the REMEDY engine selects a corrective path. This project-specific workflow is intended to reduce unnecessary rejection of products that can be relabeled or refilled.

\section{Beverage and Packaging-Oriented Machine Vision}
\label{sec:beverage-packaging}

Beverage inspection presents specific visual challenges. Transparent bottles produce reflections and refractions, fill lines may be faint, labels may wrap around curved surfaces, and caps or seals can be misaligned by only a few pixels. Farhangi et al. addressed cap defects, liquid level, and label placement in liquid bottles using LabVIEW, a CMOS camera, edge detection, and pattern matching \cite{farhangi2024machine}. The reported average accuracy of 95.6\% shows that classical machine vision can still perform well in constrained bottle-inspection tasks.

The limitation of traditional machine vision is adaptability. A rule tuned for one bottle shape, cap color, or label position may fail after a packaging redesign or SKU change. This motivates the product-profile approach in \shorttitle. Instead of hard-coding one global rule set, each SKU can define expected regions of interest, fill bands, label boundaries, severity thresholds, and remediable actions. In the current project plan, YAML profiles are used for bottle\_250ml, can\_330ml, and pouch\_100g, allowing the same inference pipeline to behave differently for different product formats.

The thermoforming study by Banus et al. also offers a practical lesson for packaging inspection: the vision system, imaging configuration, and dataset design are as important as the network architecture \cite{banus2021thermoforming}. Their use of production-line imaging and rigorous rejection criteria supports the need for a data-centric pipeline in \shorttitle. Similarly, Xiong et al. emphasize that food packaging quality involves both visual conditions and informational correctness \cite{xiong2024artifact}. This supports the inclusion of label errors in the target defect taxonomy.

\section{IoT and Sensor Fusion Systems}
\label{sec:iot-sensor-fusion}

Several papers expand quality inspection beyond RGB images by incorporating sensors, cloud systems, and edge analytics. Krishnan et al. proposed an AI-driven intelligent IoT system for real-time food quality monitoring along the supply chain \cite{krishnan2024aiot}. Their CNN-based system reported 95.2\% accuracy, 92.8\% precision, 94.5\% recall, and 93.6\% F1-score, with monitoring results across transport, storage, and processing stages. This shows the value of combining AI models with environmental data such as temperature, humidity, and chemical composition.

Du proposed an optimized online measurement and detection system for intelligent manufacturing using multi-sensor fusion, machine learning, adaptive control, fault prediction, and edge computing \cite{du2025online}. The paper reported 98\% surface defect detection accuracy, about 60\% improvement in production efficiency, and a 4.85\% reduction in scrap rate. Although the domain is general manufacturing, the results support the importance of online inspection and low-latency edge processing.

Cao et al. developed an RFID tag-integrated multi-sensor system with an AIoT cloud platform for food quality analysis \cite{cao2025rfid}. Their system uses semi-passive UHF RFID tags, temperature/humidity/CO2 sensing, cloud visualization, and LSTM-based quality prediction. This approach addresses internal and environmental product quality attributes that cameras may not see. However, cloud-based and sensor-heavy systems can introduce cost, calibration, maintenance, and latency concerns. For \shorttitle, RGB vision is the initial focus because it is easier to deploy in a capstone prototype and aligns with available annotation tools. The architecture still leaves room for future sensor values to be passed into REMEDY as additional severity signals.

\section{Specialized, Adaptive, and Weakly Supervised Methods}
\label{sec:specialized-methods}

Defective samples are often scarce in food and beverage manufacturing because serious defects are rare and may not be consistently captured. Truong and Luong proposed an autoencoder-based, non-destructive approach for detecting defects and contamination in reusable food packaging \cite{truong2024autoencoder}. Their method trains primarily on clean samples and detects anomalies through reconstruction differences. This reduces dependence on labeled defect images and is highly relevant when collecting examples of rare contamination or cracking is difficult. The trade-off is that anomaly thresholds can be sensitive and may produce false positives if normal product variation is not well modeled.

Ribeiro et al. proposed automatic visual inspection for industrial applications using incremental learning and a multi-view setup for pharmaceutical bottles \cite{ribeiro2025automatic}. Incremental learning is important because real factories encounter new defects after deployment. Full retraining is expensive and can cause downtime, while learning without forgetting aims to add new defect knowledge without destroying prior performance. This is relevant to \shorttitle\ because food and beverage SKUs, labels, and packaging materials change over time.

Xu et al. introduced a multiple-instance-learning approach for efficient online surface defect detection using image-level labels \cite{xu2024mil}. This paper is only partially aligned with food and beverage quality inspection, but it is valuable because it addresses the labeling burden. Fine-grained bounding-box annotation is costly, and weak supervision may help bootstrap inspection models when only image-level pass/fail data are available. The limitation is that approximate localization from feature maps may not be precise enough for operator-facing defect evidence.

Liakos et al. conducted a systematic review of machine learning for quality control in the food industry \cite{liakos2025review}. The review identifies neural networks as a dominant approach and highlights trends such as hyperspectral imaging, sensor fusion, explainable AI, and blockchain-enabled traceability. It also identifies data scarcity, domain coverage bias, regulatory compliance, and integration with legacy systems as persistent challenges. This synthesis strongly supports the motivation for a transparent, auditable inspection architecture.

\section{Comparative Summary of Reviewed Papers}
\label{sec:comparative-summary}

Table~\ref{tab:literature-comparison} summarizes the papers consulted for this project. The comparison focuses on method, domain, contribution, and limitation because these aspects directly inform the design decisions for \shorttitle. The final column is written from the viewpoint of the proposed system rather than as a generic criticism of each paper.

\renewcommand{\arraystretch}{1.18}
\small
\begin{table}[htbp]
\centering
\caption{Comparative summary of papers consulted for \shorttitle\ (Part 1)}
\label{tab:literature-comparison}
\begin{tabular}{|P{2.4cm}|P{2.5cm}|P{2.6cm}|P{2.8cm}|P{2.8cm}|}
\hline
\textbf{Study} & \textbf{Domain} & \textbf{Method} & \textbf{Key Contribution} & \textbf{Limitation for This Project} \\
\hline
Chatchapol et al. \cite{chatchapol2024liquid} & Beverage filling & CNN with camera module and conveyor speed control & 98.89\% liquid-level accuracy and 100\% lid sealing accuracy in beverage-line context & Focuses on liquid level and sealing; limited multi-defect and XAI support \\
\hline
Kavitha and Mamatha \cite{kavitha2024automated} & Manufacturing survey & YOLOv5 and image processing survey & Shows YOLOv5 suitability for fast defect detection and sorting & Survey-level treatment; not food or beverage specific \\
\hline
Sanjay \cite{sanjay2024foodoil} & Food oil quality & CNN image classifier & Demonstrates AI-based visual assessment for food oil quality & Narrow food-oil scope; no localization or operator traceability \\
\hline
Kavitha et al. \cite{kavitha2024deeplearning} & Smart manufacturing & YOLO with Arduino sorting & End-to-end detection and physical segregation concept & Low-cost actuation may not scale to industrial food lines \\
\hline
Farhangi et al. \cite{farhangi2024machine} & Liquid bottles & LabVIEW machine vision, edge detection, pattern matching & Inspects cap defects, liquid level, and label placement with 95.6\% average accuracy & Rule-based approach requires careful tuning for each SKU \\
\hline
Nguyen et al. \cite{nguyen2025industrial} & Industrial surface inspection & YOLOv8, multi-threading, PLC integration & Demonstrates real-time machine-vision system with above 90\% accuracy & Generic industrial products; no food-safety explainability layer \\
\hline
\end{tabular}
\end{table}

\begin{table}[htbp]
\centering
\caption{Comparative summary of papers consulted for \shorttitle\ (Part 2)}
\begin{tabular}{|P{2.4cm}|P{2.5cm}|P{2.6cm}|P{2.8cm}|P{2.8cm}|}
\hline
\textbf{Study} & \textbf{Domain} & \textbf{Method} & \textbf{Key Contribution} & \textbf{Limitation for This Project} \\
\hline
Krishnan et al. \cite{krishnan2024aiot} & Food supply chain & CNN with IoT sensor monitoring & Reports 95.2\% accuracy and robust monitoring across transport, storage, and processing & Sensor-focused monitoring; not localized visual defect inspection \\
\hline
Truong and Luong \cite{truong2024autoencoder} & Reusable food packaging & Autoencoder anomaly detection and knowledge transfer & Detects contamination and cracks using mostly clean samples & Threshold sensitivity and false positives remain practical risks \\
\hline
Du \cite{du2025online} & Intelligent manufacturing & Multi-sensor fusion, ML, adaptive control, edge computing & Reports 98\% defect accuracy and improved production efficiency & General manufacturing; implementation complexity is high \\
\hline
Cao et al. \cite{cao2025rfid} & Food quality monitoring & RFID multi-sensors, AIoT cloud, LSTM & Enables item-level food quality tracking with cloud analytics & Adds hardware, calibration, and cloud-latency concerns \\
\hline
Liakos et al. \cite{liakos2025review} & Food quality control & PRISMA systematic review & Identifies trends in ML, XAI, sensor fusion, and traceability; highlights beverage gap & Review paper; does not implement an inspection architecture \\
\hline
Ribeiro et al. \cite{ribeiro2025automatic} & Pharma\-ceutical bottles & Multi-view visual inspection and incremental learning & Supports adaptation to new defects without full retraining & Pharmaceutical focus; needs adaptation for food and beverage SKUs \\
\hline
\end{tabular}
\end{table}

\begin{table}[htbp]
\centering
\caption{Comparative summary of papers consulted for \shorttitle\ (Part 3)}
\begin{tabular}{|P{2.4cm}|P{2.5cm}|P{2.6cm}|P{2.8cm}|P{2.8cm}|}
\hline
\textbf{Study} & \textbf{Domain} & \textbf{Method} & \textbf{Key Contribution} & \textbf{Limitation for This Project} \\
\hline
Subramanian et al. \cite{subramanian2025resnet} & Manufacturing defects & Optimized ResNet50 transfer learning & Shows value of residual CNNs for cracks, dents, and misalignments & Classification-heavy; edge deployment may be difficult \\
\hline
Xiong et al. \cite{xiong2024artifact} & Food packaging & Data-centric CV framework with DL and traditional CV & Covers visual and informational packaging quality factors & Framework does not provide the proposed REMEDY triage layer \\
\hline
Banus et al. \cite{banus2021thermoforming} & Thermo\-forming food packages & DenseNet, ResNet, VGG comparison & Production-line validation with very low false reject and false accept rates & Focused on thermoforming seals, not broad food and beverage formats \\
\hline
Gediya \cite{gediya2024smart} & Smart manufacturing & AI image recognition, IoT, edge computing & Presents a holistic Industry 4.0 quality-control framework & Lacks detailed food/beverage dataset and defect taxonomy \\
\hline
Liu et al. \cite{liu2025foodpkg} & Food packaging & Improved YOLOv5 with CBAM, FPN, PANet, ASFF & Closest prior work; 7400 images and 0.96 accuracy for packaging defects & No XAI, remediation guidance, or product-profile-driven routing \\
\hline
Xu et al. \cite{xu2024mil} & Industrial surface defects & Multiple instance learning with CNN/\allowbreak ResNet50 & Reduces need for fine-grained labels and supports approximate localization & Partially relevant; localization may be insufficient for audits \\
\hline
\end{tabular}
\end{table}
\normalsize
\renewcommand{\arraystretch}{1}

\section{Synthesis and Research Gaps}
\label{sec:research-gaps}

The reviewed literature shows that automated quality inspection has matured from rule-based computer vision toward deep learning and connected manufacturing systems. However, the findings also show several gaps that motivate \shorttitle.

\begin{enumerate}
  \item \textbf{Limited beverage-specific research:} Beverage inspection appears in only a few papers, mainly liquid-level, cap, and label inspection \cite{chatchapol2024liquid,farhangi2024machine}. The broader food QC review also shows that beverages are underrepresented in ML-based food quality control research \cite{liakos2025review}.
  \item \textbf{Single-defect or narrow-product focus:} Many systems inspect one defect category, one package format, or one production setting. Real plants require inspection across fill level, caps, labels, package damage, and contamination.
  \item \textbf{Insufficient integration of localization, explanation, remediation, and traceability:} YOLO-based systems localize defects, but few convert detections into explainable visual evidence, severity scoring, recommended action, and auditable operator logs.
  \item \textbf{Edge deployment constraints:} High-performing CNNs and detector models can be computationally expensive. Production inspection requires low-latency inference and reliable operation on edge hardware or near-line GPUs.
  \item \textbf{Dataset scarcity and annotation burden:} Food and beverage defects are rare, varied, and often proprietary. Autoencoders, weak supervision, and data-centric AI help reduce this burden, but they must be balanced against false positives and audit requirements \cite{truong2024autoencoder,xu2024mil,xiong2024artifact}.
\end{enumerate}

\section{Need for the Proposed System}
\label{sec:need-proposed-system}

The literature supports the need for a system that combines the strengths of existing approaches while addressing their operational gaps. CNNs and transfer learning provide strong image understanding, YOLO models provide real-time localization, IoT studies demonstrate the value of traceable quality data, and specialized methods help handle scarce defect samples. \shorttitle\ builds on these directions by proposing a product-profile-driven architecture with configurable SKU rules, YOLOv8 localization, MobileNet V3 confirmation, Monte Carlo Dropout uncertainty, REMEDY triage, and a web dashboard for inspection review.

The central design choice is to separate product context from defect localization. Instead of forcing the vision model to infer product type in every frame, the production line or operator selects an SKU profile that defines expected regions, acceptable limits, defect classes, severity rules, and possible corrective actions. This makes the system more practical for transparent bottles, cans, wrappers, and boxes, where visual characteristics differ substantially. The literature review therefore establishes both the feasibility of deep learning based inspection and the need for an integrated, auditable, operator-facing pipeline.

For the capstone implementation, this means that a detected label error on a bottle may be routed to a relabel station if severity is S1 or S2, while visible contamination may be quarantined as S4 because it is a food-safety risk. Similarly, an uncertain low-confidence prediction is not silently accepted or rejected; it is marked for review so that the operator can inspect the image, bounding box, and model confidence. This is the main difference between \shorttitle\ and prior work that ends at a detector output.

\section{Chapter Summary}
\label{sec:literature-summary}

This chapter reviewed the research papers consulted for the project and grouped them into CNN and transfer learning methods, YOLO and object detection systems, beverage and packaging machine vision, IoT and sensor fusion, and specialized adaptive methods. The review shows that high accuracy is achievable in controlled settings, but existing work rarely combines multi-defect inspection, beverage relevance, localization, explainability, remediation, traceability, and edge-aware deployment. These gaps form the foundation for the \shorttitle\ architecture proposed in the subsequent stages of the capstone project.

% =====================================================================
% Chapter 3: Methodology
% =====================================================================
\chapter{Methodology}
\label{ch:methodology}

\section{Method Overview}
\label{sec:method-overview}

The methodology for \shorttitle{} is based on a modular inspection pipeline rather than a single end-to-end classifier. That choice was deliberate. In a food and beverage setting, the same camera frame may need to support different checks depending on the product family, the packaging format, and the defect class under review. A rigid monolithic model would blur those distinctions and make the final decision harder to explain. The architecture used in this project instead separates product routing, defect detection, evidence combination, confidence handling, and remediation so that each step can be inspected independently.

At a practical level, the workflow starts with a captured RGB frame, assigns the frame to the appropriate product branch, executes the relevant inspection modules, and then consolidates the resulting evidence into a verdict. The pipeline is therefore not only concerned with whether a defect exists. It is also concerned with what the defect is, where it is located, how confident the system is, and what the next action should be. That structure is important because the intended user is not a data scientist but an operator who needs a clear and traceable answer.

The two figures shown here serve as the main architectural references for this chapter. The first image presents the product routing and inspection path. The second image shows how outputs are combined, scored, and routed into the REMEDY layer for final handling.

\section{Design Principles}
\label{sec:design-principles}

Three design principles guided the methodology.

First, the system is product aware. Food products and beverage products do not fail in the same way, so the inspection logic cannot be identical. A snack wrapper, a rigid can, a carton, and a transparent bottle each expose different visual cues. The pipeline therefore treats product type as a first-class input instead of trying to infer everything from a single detector output.

Second, the system is evidence driven. Every decision should be grounded in visible image evidence rather than only in a scalar score. Bounding boxes, confidence values, uncertainty estimates, and severity outputs are preserved so that the final result can be audited later.

Third, the system is fail-safe rather than optimistic. If a product region is uncertain, if the fill level cannot be observed clearly, or if the label evidence does not match the SKU profile, the pipeline should not silently force a pass. The design favors review, escalation, or rejection when the evidence is incomplete.

\section{System Architecture}
\label{sec:system-architecture}

The system architecture is organised into three logical layers: input acquisition, inspection, and decision support. The input layer captures a single camera frame from the production line or development setup. The inspection layer isolates the product, runs the appropriate detectors, and converts raw image findings into defect objects. The decision-support layer uses uncertainty and severity logic to decide whether the item should pass, be escalated, or be routed to corrective handling.

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.92\textheight,keepaspectratio]{\detokenize{../Presentation/VisionFood QAI Product-2026-05-04-140725.png}}
\caption{Primary system architecture showing product routing and inspection flow.}
\label{fig:primary-system-architecture}
\end{figure}
\clearpage

Figure~\ref{fig:primary-system-architecture} shows the first stage of the architecture in operational terms. The image is not just a diagram of model blocks; it represents the actual inspection route. A frame enters the system, the product context is identified, and the frame is passed to the relevant branch. From there, the branch-specific checks are applied. For food products, the system focuses on packaging integrity and label quality. For beverage products, the system focuses on bottle geometry, cap fitting, fill level, and surface condition.

This separation reduces unnecessary computation and avoids applying irrelevant checks to the wrong product type. A transparent bottle, for example, requires fill-level reasoning that is meaningless for a carton. Similarly, a carton may require corner and crease inspection that does not apply to a rigid can. The architecture therefore keeps the routing logic above the defect logic so that each product category can be inspected according to its own rules.

The first stage also aligns with the SKU profile concept used in the implementation. In practice, the selected SKU tells the system what product family it is handling, which inspection rules are valid, and which defect classes should be emphasized. This makes the workflow configurable without requiring the detector layer to be rewritten for every new package.

\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.92\textheight,keepaspectratio]{\detokenize{../Presentation/Output Combination and-2026-05-04-141327.png}}
\caption{Output combination and evaluation pipeline.}
\label{fig:output-combination-evaluation}
\end{figure}
\clearpage

Figure~\ref{fig:output-combination-evaluation} captures the downstream decision layer. After the inspection modules finish, their outputs are not consumed in isolation. Instead, the system combines the detections, evaluates how stable or uncertain the prediction is, estimates severity, and then selects the response path. That downstream logic is central to the project because two defects with the same visual appearance may require different actions depending on confidence, product profile, and operational context.

This stage also makes the pipeline easier to review. The output includes the defect evidence, the confidence context, the severity grade, and the final route. In other words, the architecture supports both machine decision-making and human review. That is important in quality inspection because operators often need to justify why a product was escalated, held, or rejected.

\section{End-to-End Workflow}
\label{sec:end-to-end-workflow}

The implementation follows a defined sequence designed to keep the inspection process deterministic and reproducible.

\begin{enumerate}
  \item A frame is captured from the imaging source and passed into the pipeline.
  \item The product context is resolved using the selected SKU profile.
  \item The appropriate inspection branch is executed for the product category and subtype.
  \item The branch returns defect objects and associated evidence.
  \item The system evaluates whether the results are confident enough for automatic handling or whether they require review.
  \item The severity and triage logic convert the inspection output into an action such as pass, escalate, rework, or reject.
  \item The complete result is stored so it can be audited or displayed in the dashboard.
\end{enumerate}

This workflow reflects the logic of an industrial inspection station. A product is not assessed by a single detector in isolation; rather, the final decision is derived from product identity, defect visibility, confidence, and corrective feasibility. Making those stages explicit improves traceability, supports debugging, and strengthens the justification for the final result during evaluation.

\section{Project Planning}
\label{sec:project-planning}

The project was organised into phases so that the scope could expand in a controlled manner without becoming fragmented. The first phase focused on problem definition and literature review. This stage clarified the relevant failure modes in food and beverage inspection, identified areas where prior work was effective, and isolated the remaining gaps. It also established that the system should support multiple package types rather than a single product configuration.

The next phase focused on architectural decomposition. Instead of organising the work around file names or model artefacts, the implementation was structured around functional inspection tasks: product routing, defect localisation, branch-specific checks, uncertainty estimation, and decision handling. This approach made the system easier to extend because each block could be implemented, tested, and documented independently.

The third phase covered data and profile preparation. In this project, data is not limited to image files. It also includes the SKU profile, the class mapping, the expected product format, and the rules that determine whether a detection is operationally significant. Addressing these dependencies early reduces the risk of developing a model that performs well in isolation but is difficult to use within the inspection pipeline.

The final phase focused on reporting and presentation. The report was written to describe the implemented pipeline rather than a hypothetical alternative, which keeps the methodology aligned with the system architecture presented in the thesis.

\section{Requirements}
\label{sec:requirements}

The project requirements were derived from the operational needs of a real inspection workflow. They are grouped into functional, non-functional, software, development tooling, hardware, data, and operational requirements so that each aspect of the system can be evaluated independently.

\subsection{Functional Requirements}
\label{subsec:functional-requirements}

The system had to detect the product in the image, inspect the relevant defect types, and produce a final verdict. It also had to support separate food and beverage branches, generate explainable output, preserve the inspection evidence, and retain the result for later review in the dashboard or database.

In addition, the pipeline had to support product-aware rules. That means the same image could lead to different outcomes depending on the SKU profile, the expected defect classes, and the allowable limits for that product.

\subsection{Non-Functional Requirements}
\label{subsec:non-functional-requirements}

The system had to be modular, accurate, and fast enough for inspection use. It also had to remain interpretable for operators, tolerant of variable lighting and packaging appearance, and structured so that future updates could be added without replacing the full pipeline.

Traceability was another important non-functional requirement. The output should support later review by keeping the defect class, score, and decision context together instead of collapsing everything into a single label.

\subsection{Software Requirements}
\label{subsec:software-requirements}

The implementation required Python-based computer vision support, deep-learning inference tooling, image-processing libraries, and a backend capable of storing and serving inspection results. The report and presentation were prepared in LaTeX, which made it possible to keep the documentation structured and consistent with the thesis format.

The software stack also had to support future extension. For that reason, the methodology assumes clear schema definitions, reusable inspection modules, and a pipeline interface that can evolve without forcing the UI or database layer to change in lockstep.

\subsection{Development Tooling Requirements}
\label{subsec:development-tooling-requirements}

Version control with Git was required so that changes to the code, configuration, and documentation could be tracked systematically across the life of the project. A versioned workflow also provided a reliable way to compare revisions, isolate experimental changes, and preserve stable checkpoints when the implementation evolved.

Roboflow was required for dataset annotation and management. It provided a structured environment for drawing bounding boxes, organising defect classes, and exporting annotations in a consistent format for downstream training and evaluation. This requirement is important because inspection performance depends heavily on annotation quality and class consistency.

\subsection{Hardware Requirements}
\label{subsec:hardware-requirements}

The methodology assumes a camera-based imaging setup and a compute device capable of running the inspection pipeline at practical speed. Depending on deployment, this could be a development workstation during experimentation or an edge-style inference device in a production setting.

For beverage inspection, the imaging setup must expose enough contrast for fill-level reasoning. For packaging inspection, the lighting must be stable enough to preserve contours, labels, and visible surface defects.

\subsection{Data Requirements}
\label{subsec:data-requirements}

The system required annotated product images covering both normal and defective samples. The data also had to represent the package types used in the architecture so that each inspection branch could be evaluated on the correct visual material.

Data quality mattered as much as data quantity. If the annotations are inconsistent, the downstream defect logic becomes unreliable. For that reason, the dataset requirements include consistent class names, visible defect regions, and product formats that match the SKU profiles used in the pipeline.

To support this process, images were prepared and labeled in Roboflow before being exported for training and evaluation. This ensured that the annotation format remained uniform across defect categories and that the resulting dataset could be reused across experiments without reworking labels each time.

\subsection{Operational Requirements}
\label{subsec:operational-requirements}

The output had to support operator review, traceability, and actionability. In practice, that means the system needed to provide more than a pass or fail answer. It needed to show what was detected, where it was detected, how strong the evidence was, and why the final decision was selected.

This requirement is especially important for uncertain or borderline cases. If the evidence is incomplete, the system should communicate that clearly instead of hiding it behind an overly confident verdict.

\section{Validation Strategy}
\label{sec:validation-strategy}

The methodology was also planned with verification in mind. Each inspection branch should be testable on its own using synthetic or controlled images, and the final pipeline should be checked end to end with sample frames that exercise the routing logic, the evidence combination stage, and the verdict rules.

The validation strategy therefore focuses on three questions: whether the correct branch is selected, whether the correct defect evidence is extracted, and whether the final result is consistent with the configured thresholds and product rules. That approach helps ensure that the pipeline remains stable as additional product categories or defect classes are added later.

% =====================================================================
% Appendices
% =====================================================================
% ---------------------------------------------------------------------
% Chapter 4: Algorithm and Integration
% ---------------------------------------------------------------------
\chapter{Algorithm, Integration And Testing}
\label{ch:algorithm-integration}

\section{Overview}
\label{sec:alg-overview}

This chapter describes the implemented inference flow used for bottle inspection and the component-level integration needed to run it end to end. The pipeline combines a YOLO detector for bottles and caps (with a two-pass zoom for small caps), a MobileNetV3-Small classifier for cap quality, and a water-surface detector plus fill-ratio math for fill-level verdicts. The presentation follows a formal, implementation-aware style: inputs and outputs are specified, the core steps are enumerated, key equations are shown, and integration interfaces are defined for testing and deployment.

\section{Algorithm}
\label{sec:algorithm}

\subsection{Notation, inputs and outputs}
\label{subsec:notation}

Let an input camera frame be $I\in\mathbb{R}^{H\times W\times 3}$. The detector produces a set of candidate bounding boxes
$\mathcal{B}=\{b_i\}_{i=1}^{N}$ where $b_i=\{x_{1},y_{1},x_{2},y_{2},c_i,cls_i\}$ with confidence $c_i$ and class $cls_i$.
Each downstream module maps a crop $C(b_i)=I[y_{1}:y_{2},x_{1}:x_{2}]$ to a structured result. Example output schemas used in the pipeline are:

\begin{itemize}
  \item Detection: $\{b_i, c_i, cls_i\}$
  \item Cap classification: $\{label_{cap}, p_{cap}\}$ (cap label and confidence)
  \item Fill-level estimate: $\{f_{fill}, v_{fill}\}$ (fill ratio and verdict)
  \item Per-bottle result: $\{bottle\_bbox, cap\_verdict, fill\_verdict, latency\_ms\}$.
\end{itemize}

\subsubsection*{Detailed notation and symbols}
For clarity we summarize the symbols and data structures used throughout the algorithm descriptions and equations.
\begin{description}
  \item[$I$:] Input image frame, shape $H\times W\times 3$.
  \item[$b_i$:] Bounding box for detection $i$, coordinates in pixel space $b_i=(x_1,y_1,x_2,y_2)$.
  \item[$c_i$:] Detector confidence for box $b_i$ (scalar in $[0,1]$).
  \item[$cls_i$:] Predicted class label for box $b_i$ (categorical index).
  \item[$C(b_i)$:] Image crop extracted from $I$ using box $b_i$.
  \item[$\mathcal{B}$:] Set of boxes returned by the detector for a frame.
  \item[$y_{top}, y_{bot}$:] Corrected bottle bounds after trimming the detector box.
  \item[$y_{water}$:] Vertical coordinate of the detected water surface (bbox center).
  \item[$f_{fill}$:] Fill ratio computed from the bottle height and water surface location (0 = empty, 1 = full).
  \item[$v_{fill}$:] Fill-level verdict in \{underfill, normal, overfill\}.
  \item[$p_{cap}$:] Cap classifier confidence for the selected cap crop.
  \item[$\tau_{under}, \tau_{over}$:] Decision thresholds for underfill and overfill.
  \item[$\alpha$:] Zoom scale factor used in the second-pass cap search.
  \item[$r_{cap}$:] Cap-region ratio (top portion of bottle used for zoom crop).
  \item[$T_{det}, T_{cls}, T_{fill}, T_{post}$:] Latencies for detector, classifier, fill model, and postprocessing steps.
\end{description}

These symbols are used consistently in pseudocode and equations that describe the implemented pipeline.

\subsection{Training methodology}
\label{subsec:training}

Training focused on the three models used in the runtime pipeline: the bottle and cap detector, the cap-quality classifier, and the water-surface detector. Fill-level classification models are not part of the deployed inference path and are therefore omitted here.

\paragraph{Bottle and cap detector (YOLO)}
The detector is trained to localize bottles and caps from full-frame images. Training uses standard YOLO augmentation defaults and class-weighting for imbalance, with best-checkpoint selection based on validation mAP. The inference pipeline then applies a two-pass zoom to improve cap recall on small objects.

\paragraph{Water-surface detector (YOLO)}
A dedicated detector is trained to localize the liquid-air interface as a single class \texttt{water\_surface}. The model outputs a tight bounding box around the visible surface line. During inference, the most plausible surface candidate within each bottle is selected and converted into a fill ratio.

\paragraph{Cap quality classifier (MobileNetV3-Small)}
The cap classifier is MobileNetV3-Small with a compact head. Training uses letterbox resizing, ImageNet normalization, and heavy augmentation (motion blur, glare simulation, color shift, and random erasing) to reflect conveyor motion and lighting variation. Focal Loss ($\gamma=2.0$) and class-weighted sampling address class imbalance. The trained weights are saved as a \texttt{.pth} checkpoint and used directly during inference.

\paragraph{Data and annotation protocol}
Annotations follow YOLO-style bounding boxes for bottle, cap, and water-surface classes. Data splits follow a 70:15:15 pattern (train:val:test), and augmentation is used to expand rare defect examples while preserving label integrity.

\paragraph{Validation and selection}
Validation metrics include mAP for detectors and accuracy, precision, recall, and F1-score for the cap classifier. The best checkpoints are selected based on validation performance and used in the inference pipeline.

\subsection{High-level algorithm}
\label{subsec:highlevel}

The per-frame algorithm is summarized in steps and in the pseudocode listing that follows.

\begin{lstlisting}[caption={Per-frame inspection pseudocode},language=Python]
def inspect_frame(I, det_model, fill_model, cap_cls=None):
  bottles, caps = detect_bottles_and_caps(I, det_model)
  results = []
  for b in bottles:
    cap = select_cap_for_bottle(b, caps)
    cap_verdict, cap_conf = classify_cap_if_available(I, cap, cap_cls)
    water = detect_water_surface(I, fill_model, b)
    fill_ratio, fill_verdict = compute_fill_ratio_and_verdict(b, water)
    results.append({
      "bottle": b,
      "cap_verdict": cap_verdict,
      "cap_conf": cap_conf,
      "fill_ratio": fill_ratio,
      "fill_verdict": fill_verdict,
    })
  return results
\end{lstlisting}

\subsection{Detector and small-object handling}
\label{subsec:detector}

The detector is YOLO-based and is responsible for proposing bottle and cap regions. Small-object handling for caps uses an explicit two-pass zoom strategy:

\begin{enumerate}
  \item Pass 1: run the detector on the full frame to obtain bottles and any high-confidence caps.
  \item If pass-1 cap confidence is low, crop the top $r_{cap}$ portion of each bottle with padding and resize by a zoom factor $\alpha$.
  \item Pass 2: re-run the detector on the zoomed crop to recover small caps with higher effective resolution.
  \item Merge pass-1 and pass-2 caps using IoU and keep the higher-confidence box.
  \item Associate the final cap to a bottle by horizontal alignment and minimum distance to the bottle top.
\end{enumerate}

This two-pass approach compensates for small-object limits in single-shot detection and improves cap recall without requiring a heavier detector.

\subsection{Fill-level estimation algorithm}
\label{subsec:fill-algo}

The implemented fill-level estimator uses a water-surface detector and geometric ratio computation. The goal is to convert a detected liquid-air interface into a fill ratio for each bottle.

\paragraph{Preprocessing} The bottle bounding box is corrected for detector padding by trimming a small fraction from the top and bottom. This produces corrected bounds $(y_{top}, y_{bot})$ that better represent the true bottle interior.

\paragraph{Surface detection} A dedicated detector finds the water-surface line as a bounding box. For each bottle, the most plausible surface candidate is selected by horizontal alignment (surface center x within the bottle bounds) and vertical inclusion (surface y within the bottle bounds). If no surface is found, the fill result is left undefined for that bottle.

\paragraph{Fill ratio} The water-surface y coordinate is taken as the center of the surface bounding box. The fill ratio is computed by
\begin{equation}
f_{fill} = \frac{y_{bot} - y_{water}}{y_{bot} - y_{top}}.
\end{equation}
This yields $f_{fill} \in [0,1]$ where 0 indicates empty and 1 indicates full, with values in between representing partial fill.

\subsection{Fill-level decision thresholds}
\label{subsec:fusion}

Fill-level decisions are derived directly from the fill ratio. Given thresholds $\tau_{under}$ and $\tau_{over}$:
\begin{equation}
v_{fill} =
\begin{cases}
\text{underfill}, & f_{fill} < \tau_{under} \\
\text{overfill},  & f_{fill} > \tau_{over} \\
\text{normal},    & \text{otherwise}
\end{cases}
\end{equation}
If no valid water-surface detection is available for a bottle, the fill-level verdict is left undefined for that instance.

\subsection{Cap quality classification}
\label{subsec:classifier}

Cap quality is verified by a MobileNetV3-Small classifier applied to a cropped cap region. The crop is letterbox-resized to preserve aspect ratio and normalized using ImageNet mean and standard deviation. The classifier outputs a label from \texttt{good\_cap}, \texttt{defective\_cap}, or \texttt{no\_cap} along with a confidence score $p_{cap}$. The verdict is mapped as follows:

\begin{itemize}
  \item \texttt{good\_cap} $\to$ \textbf{Good Cap}
  \item \texttt{defective\_cap} $\to$ \textbf{Defective Cap}
  \item \texttt{no\_cap} $\to$ \textbf{Missing Cap}
\end{itemize}

If the cap classifier is not loaded, the pipeline defaults to \textbf{Cap Present} whenever a cap detection exists and \textbf{Missing Cap} when it does not.

\section{Integration}
\label{sec:integration}

\subsection{Component interfaces}
\label{subsec:interfaces}

Each module exposes a minimal API for integration and testing. Examples (Python-like signatures):

\begin{lstlisting}[language=Python]
def detector.detect(frame: np.ndarray) -> dict   # bottles, caps, metadata
def classify_cap(classifier, device, crop: np.ndarray) -> tuple[str, float]
def compute_fill_ratio(bottle_bbox: list[int], water_bbox: list[int]) -> float
def fill_verdict(fill_ratio: float, under: float, over: float) -> str
def full_inspect(detector, fill_model, classifier, cls_device, frame) -> dict
\end{lstlisting}

The per-bottle result is serializable to JSON and contains the structured evidence used by downstream storage and UI layers.

\subsection{Packaging and deployment}
\label{subsec:packaging}

The runtime loads YOLO detector weights for bottles and caps, YOLO weights for the water-surface detector, and a MobileNetV3-Small classifier checkpoint for cap quality. The pipeline can run in batch mode on image folders or in webcam mode, producing annotated images and per-bottle JSON summaries. The inspection API endpoint accepts base64 images and returns a structured result for storage and dashboard display.

\section{User Interface Implementation}
\label{sec:ui-implementation}

The implemented dashboard provides an operator-facing interface around the inspection pipeline. It is built as a React and Vite application with TypeScript types aligned to the backend inspection schema. The interface includes an operations overview, live monitor, manual inspection panel, inspection history, escalation queue, product management, report generation, and settings workflows.

The manual inspection panel allows an operator to upload an image, enter SKU and product information, run inspection, and view the returned verdict. The result view displays verdict badges, severity information, latency, annotated image overlays, detection details, REMEDY action, and uncertainty information. This supports manual validation because the user can inspect both the decision and the evidence in the same screen.

The live monitor and run setup components support production-line operation. The run setup banner shows whether a production run is active, lets an operator start a run, and allows a supervisor or administrator to end or abort a run. The active-run view displays SKU, product name when available, product sub-type badge, container contents badge, inspection count, defect count, and elapsed duration. The history and detail views support traceability by displaying stored inspection results, annotated images, detections, severity assessment, remediation action, and label or QR information when available.

\subsection{Observability and logging}
\label{subsec:observability}

Key runtime metrics for monitoring and testing include per-frame latency, bottle count, cap-detection pass-1 and pass-2 counts, cap verdict distribution, and fill-ratio statistics. These metrics support post-deployment calibration and automated test assertions.

\subsection{Testing hooks and validation points}
\label{subsec:testing-hooks}

Testing follows the modular structure of the pipeline so that each stage can be verified in isolation before end-to-end runs. The scripts are designed to accept a single image, a folder of images, or a live webcam stream and produce both annotated outputs and structured per-bottle summaries. This makes regression checks repeatable and keeps failures local to the responsible module.

Key testing scripts and their roles are:
\begin{itemize}
  \item \texttt{detect\_cap\_presence.py}: quick binary check for cap presence using the bottle+cap detector; validates class mapping and detector thresholds.
  \item \texttt{only\_yolo.py}: detector-only pipeline with the two-pass zoom logic; stresses small-cap recovery and pass-1/pass-2 merging.
  \item \texttt{yoloWithClassifier.py}: integrates the MobileNetV3 cap classifier with detection to validate crop quality, preprocessing, and classifier confidence.
  \item \texttt{yoloWithFillLevel.py}: full bottle pipeline that adds water-surface detection and fill-ratio computation; used to verify fill thresholds and reporting.
\end{itemize}

Validation uses synthetic frames with known geometry for detector checks, controlled fill-level images to verify $f_{fill}$ and thresholding, and small-object stress sets to validate the crop-and-zoom logic. The resulting annotated images and per-bottle JSON summaries are used as regression fixtures in the dedicated Testing chapter.

\section{Cap cropping and small-object detection: detailed explanation}
\label{sec:cap-cropping}

Cap detection often fails when caps occupy only a few pixels in the full frame. To mitigate this, the implemented pipeline uses a crop-and-zoom refinement with the following steps:

\begin{enumerate}
  \item Base detection: run YOLO on the full frame to obtain bottle and cap candidates.
  \item Skip check: if a cap is already detected with high confidence, skip the zoom pass.
  \item Cap-region crop: for each bottle, crop the top $r_{cap}$ portion with padding to include context.
  \item Zoom and re-detect: resize the crop by factor $\alpha$ and run YOLO again to recover small caps.
  \item Merge detections: combine pass-1 and pass-2 caps using IoU and keep the stronger box.
\end{enumerate}

This strategy reduces false negatives for small caps while keeping the detector lightweight.

\section{Complexity and runtime notes}
\label{subsec:complexity}

Per-frame time complexity is dominated by model inference. Let $T_{det}^{(1)}$ be the full-frame detector latency, $T_{det}^{(2)}$ the zoomed cap pass, $T_{cls}$ the cap classifier latency, and $T_{fill}$ the water-surface detector latency. With $N_b$ bottles in a frame, the worst-case latency is approximately
\begin{equation}
T_{frame} \approx T_{det}^{(1)} + N_b\cdot T_{det}^{(2)} + N_b\cdot T_{cls} + N_b\cdot T_{fill} + T_{post}
\end{equation}
where $T_{post}$ includes cropping, resizing, and result assembly. In practice $N_b$ is small and the zoom pass is skipped when caps are already detected with high confidence.

% ---------------------------------------------------------------------
% Chapter 5: Results and Discussion
% ---------------------------------------------------------------------
\chapter{Results and Discussion}
\label{ch:results-discussion}

\section{Overview}
\label{sec:results-overview}

This chapter presents the implemented results obtained from the \shorttitle\ pipeline and discusses the practical behaviour observed during system validation. The evaluation focuses on the working inspection path for transparent beverage bottles, cap-quality verification, fill-level estimation, REMEDY routing, API integration, and database traceability.

The implemented system converts an input product image into a structured inspection result. The result contains detected defect evidence, confidence values, a final verdict, latency metadata, and visual overlays for operator review. This design makes the output useful not only for model evaluation but also for actual factory workflow, because each automated decision can be traced to a visible detection and a stored inspection record.

\section{Experimental Setup}
\label{sec:experimental-setup}

The project repository contains model weights, annotated inference outputs, dashboard source code, API routes, database repositories, and reporting utilities. The main evaluation context used in this phase is transparent-bottle inspection, where the system checks cap status and fill-level behaviour. Training and validation assets are stored under the \texttt{runs} directory, including the cap-inspection annotations, trained detector weights, inference predictions, and training metrics.

The cap detector training run was executed using the \texttt{a5000-balanced} training profile. The recorded hardware configuration includes an NVIDIA RTX A5000 GPU with approximately 24 GB of VRAM and 128 GB of system RAM. The training configuration used image size 736, batch size 16, 16 workers, RAM caching, automatic mixed precision, cosine learning-rate scheduling, multi-scale training, and mosaic closing near the end of training. The cap-related classes in the recorded training context are \texttt{defectCap}, \texttt{goodCap}, and \texttt{noCap}.

\begin{table}[h!]
\centering
\caption{Cap-inspection training context}
\label{tab:cap-training-context}
\begin{tabularx}{\textwidth}{|P{4cm}|Y|}
\hline
\textbf{Item} & \textbf{Recorded value} \\
\hline
Product category & Beverage \\
\hline
Product sub-type & Transparent bottle \\
\hline
Inspection focus & Cap integrity \\
\hline
Model classes & \texttt{defectCap}, \texttt{goodCap}, \texttt{noCap} \\
\hline
Image size & 736 pixels \\
\hline
Batch size & 16 \\
\hline
Training profile & \texttt{a5000-balanced} \\
\hline
Hardware & NVIDIA RTX A5000 GPU, approximately 24 GB VRAM \\
\hline
\end{tabularx}
\end{table}

\section{Model Training Results}
\label{sec:model-training-results}

The final recorded epoch of the cap-inspection detector training run shows strong localisation and classification performance for the bottle-cap task. The recorded precision was 0.8691, recall was 0.9729, mAP@0.5 was 0.9583, and mAP@0.5:0.95 was 0.7861. These values indicate that the model is especially strong at recovering relevant cap instances, which is important in a safety-critical inspection pipeline where missed defects can allow a non-compliant product to pass.

\begin{table}[h!]
\centering
\caption{Final recorded detector metrics for cap inspection}
\label{tab:cap-detector-metrics}
\begin{tabularx}{\textwidth}{|P{4cm}|P{4cm}|Y|}
\hline
\textbf{Metric} & \textbf{Recorded value} & \textbf{Interpretation} \\
\hline
Precision & 0.8691 & Most positive detections correspond to a valid cap-status class. \\
\hline
Recall & 0.9729 & The model recovers a high proportion of relevant cap instances. \\
\hline
mAP@0.5 & 0.9583 & Bounding-box detection is strong at the IoU 0.5 threshold. \\
\hline
mAP@0.5:0.95 & 0.7861 & Localisation remains useful across stricter IoU thresholds. \\
\hline
\end{tabularx}
\end{table}

The high recall is especially valuable for the implemented two-stage cap inspection logic. A missed cap defect has higher operational cost than a moderate number of false positives, because the REMEDY and review layers can still route uncertain products for review, while a missed defect may continue downstream. The model therefore supports the system objective of prioritising reliable defect capture over purely aesthetic prediction output.

\section{Inspection Output Validation}
\label{sec:inspection-output-validation}

The inference validation outputs stored in the repository show that the detector produces structured per-image records containing the source image path, product category, product sub-type, inspection focus, defect status, detection count, class name, confidence, bounding box, normalised bounding box, and area fraction. This confirms that the inspection pipeline produces both visual and machine-readable evidence. Such structured output is required for later aggregation in analytics, report generation, and traceable decision review.

The validation examples show three important cases:
\begin{itemize}
  \item Defective caps are detected as \texttt{defectCap} with bounding boxes and confidence values.
  \item Correct caps are detected as \texttt{goodCap}, allowing the system to distinguish between actual defects and acceptable products.
  \item Missing-cap examples are represented through the \texttt{noCap} class, which enables direct routing to rejection or operator review depending on confidence and severity.
\end{itemize}

The fill-level path was validated through the algorithm described in Chapter~\ref{ch:algorithm-integration}. The water-surface detector localises the liquid-air interface, after which the geometric fill ratio is calculated using the corrected bottle top, bottle bottom, and detected surface coordinate. The output is converted into one of three verdicts: \texttt{underfill}, \texttt{normal}, or \texttt{overfill}. This separation between detection and measurement is useful because the surface detector supplies visual evidence, while the final fill decision is derived from a transparent equation.

\section{System-Level Results}
\label{sec:system-level-results}

At the system level, the project demonstrates an end-to-end inspection workflow rather than an isolated model. A camera or uploaded image enters the API, the inference pipeline produces detections and a verdict, and the REMEDY engine scores severity. The FastAPI inspection route accepts base64-encoded JPEG or PNG images and returns a structured \texttt{InspectionResult}. The database layer stores inspection history, defect details, remediation actions, product information, and production run metadata.

The system supports the following output behaviours:
\begin{itemize}
  \item Clean images produce a \texttt{PASS} verdict when no defect evidence is detected.
  \item High-confidence defects produce \texttt{FAIL} or escalation-oriented verdicts depending on uncertainty and threshold configuration.
  \item REMEDY maps common defect types to corrective actions such as \texttt{RELABEL}, \texttt{REFILL}, \texttt{REPACK}, \texttt{CLEAN}, or \texttt{REJECT}.
  \item Inspection history remains available for later review, analytics, and report generation.
\end{itemize}

This result is important because a manufacturing inspection system must do more than classify an image. It must decide what the line should do next, preserve evidence, and support accountability when an automated decision is questioned.

\section{Discussion}
\label{sec:results-discussion-section}

The results show that \shorttitle\ is strongest when treated as an integrated quality-inspection workflow. The detector provides visual evidence, the fill-level logic provides measurable compliance checks, and the REMEDY engine turns defect evidence into action. This integration is the main achievement of the project.

The main limitation is that the available training evidence is stronger for bottle and cap inspection than for the full range of food and packaging categories described in the broader design. Transparent bottles, cap status, and fill level are currently the most mature inspection path. Additional data collection and validation will be required for flexible wrappers, rigid cans, rigid boxes, OCR date verification, and QR mismatch handling before the same confidence can be claimed across all product types.

Another limitation is deployment maturity. The repository includes API, dashboard, database, model, report, and REMEDY components, but real production deployment would require stable camera calibration, controlled lighting, production-line timing tests, and operator acceptance testing. These items are therefore carried into the future-work plan in Chapter~\ref{ch:conclusion-future-work}.

% ---------------------------------------------------------------------
% Chapter 6: Conclusion and Future Work
% ---------------------------------------------------------------------
\chapter{Conclusion and Future Work}
\label{ch:conclusion-future-work}

\section{Conclusion}
\label{sec:conclusion}

The \shorttitle\ project demonstrates an AI-assisted quality inspection system for food and beverage manufacturing. The implemented system combines computer vision, structured inspection output, uncertainty-aware decision logic, REMEDY-based corrective routing, API integration, persistent inspection records, and an operator-facing dashboard. The project therefore addresses the central problem identified in Chapter~1: manual inspection is difficult to scale, while simple rule-based inspection lacks flexibility for modern product variation.

The work confirms that transparent-bottle inspection can be handled through a practical combination of object detection, cap-quality classification, and geometric fill-level estimation. The cap-inspection training results show strong performance, with final recorded mAP@0.5 of 0.9583 and recall of 0.9729. The fill-level method provides explainable measurement because the final decision is derived from a visible liquid-surface detection and a defined fill-ratio equation. The REMEDY engine further improves practical usefulness by converting defect evidence into actions such as refill, relabel, clean, repack, or reject.

The user interface implementation is also a significant outcome. The dashboard provides manual inspection, live production run setup, inspection history, detail review, analytics, report download, and product registration. This means the project is not limited to model experimentation; it provides a working inspection environment that can be demonstrated, operated, and extended.

\section{Key Learnings}
\label{sec:key-learnings}

The first key learning is that a quality-inspection system must be designed as a pipeline rather than as a single model. Detection accuracy matters, but the final value comes from evidence capture, decision logic, remediation, storage, and operator visibility. The second learning is that explainability can be built into system design through annotated images, explicit fill-ratio equations, confidence values, severity components, and traceable verdicts. The third learning is that product context is essential. SKU profiles, product sub-types, and container contents allow the same inspection framework to adapt to different production scenarios without changing the whole pipeline.

Another important learning is that user experience is part of the technical solution. An accurate model is difficult to trust if operators cannot understand what it detected or why it routed a product to a station. The dashboard therefore plays a direct role in reliability by making inspection evidence visible and actionable.

\section{Comparative Analysis as Future Work}
\label{sec:future-comparative-analysis}

A complete comparative analysis is planned as future work because the current phase focuses on implementation and internal validation. The future comparative study should evaluate \shorttitle\ against three baselines:
\begin{itemize}
  \item Manual visual inspection by trained operators.
  \item Classical rule-based machine vision using fixed thresholds and contours.
  \item A model-only detector without REMEDY routing, uncertainty handling, or dashboard traceability.
\end{itemize}

The comparison should measure detection accuracy, false rejection rate, false acceptance rate, average inspection latency, operator intervention rate, and remediation success rate. The expected benefit of \shorttitle\ is not only better detection, but also better operational handling of defects through severity scoring, evidence storage, and corrective routing.

\section{Optimisation Roadmap}
\label{sec:future-optimisation}

Optimisation is also reserved for future work because production optimisation should be based on larger deployment measurements rather than only laboratory examples. The first optimisation direction is model optimisation. The detector and classifier should be exported to ONNX, profiled on the target edge device, quantised where accuracy allows, and benchmarked under production image sizes. The second direction is pipeline optimisation. Detection, QR decoding, fill-level estimation, and result persistence can be parallelised where dependencies allow, while expensive modules such as OCR should remain SKU-gated and run only when required.

The third optimisation direction is camera and pre-processing optimisation. Stable lighting, fixed camera distance, automatic exposure control, and frame selection can reduce false positives and improve detection consistency.

\subsection{User Interface Optimisation}
\label{subsec:future-ui-optimisation}

User interface optimisation remains future work. The dashboard should be evaluated with operators and refined for faster review, clearer defect explanations, improved mobile responsiveness, better filtering in inspection history, and lower API load when viewing annotated media repeatedly.

\section{Future Work}
\label{sec:future-work}

The following future extensions are recommended:
\begin{itemize}
  \item Expand the dataset for flexible wrappers, rigid cans, rigid boxes, transparent bottles, label defects, contamination, and packaging damage.
  \item Add complete QR and OCR verification into the production inference path so barcode mismatch and printed-date mismatch can be handled as first-class quality defects.
  \item Validate the system on live conveyor-belt video with controlled lighting, realistic belt speed, and multiple product batches.
  \item Improve fill-level detection for difficult cases such as frosted bottles, curved bottles, reflective labels, and low-contrast liquids.
  \item Add edge deployment benchmarks on CPU-only and GPU-enabled devices to confirm latency under real production constraints.
  \item Extend REMEDY with feedback learning so repeated operator overrides can improve severity thresholds and routing rules.
  \item Optimise the user interface based on operator feedback, including inspection-history filtering, annotated-image loading, and clearer remediation prompts.
  \item Add role-based access control for operator, supervisor, and administrator workflows in the dashboard.
  \item Integrate generated daily and weekly reports with automated email or cloud storage delivery.
\end{itemize}

Future work should also include a user-interface validation study. Operators should be asked to use the dashboard on inspection examples and rate clarity, speed, confidence, and ease of corrective action.

\section{Sustainable Development Goals Alignment}
\label{sec:sdg-alignment}

The project aligns with the Sustainable Development Goals (SDGs) shown in Table~\ref{tab:sdg-alignment}. The alignment is based on the system's contribution to industrial automation, waste reduction, and food safety.

\begin{table}[h!]
\centering
\caption{SDG alignment for \shorttitle}
\label{tab:sdg-alignment}
\begin{tabularx}{\textwidth}{|P{3.3cm}|Y|P{2.2cm}|}
\hline
\textbf{SDG} & \textbf{Justification} & \textbf{Strength} \\
\hline
SDG 9: Industry, Innovation and Infrastructure &
The system contributes through AI-driven automation in manufacturing. It uses computer vision and edge deployment to enable smart factory operations. This improves inspection speed, accuracy, and scalability, leading to increased industrial productivity and technological advancement. &
High (3) \\
\hline
SDG 12: Responsible Consumption and Production &
The REMEDY engine reduces waste by identifying defects that can be corrected instead of rejected. By enabling actions like relabeling, refilling, and cleaning, the system minimises material wastage and promotes efficient resource utilisation in production processes. &
Medium (2) \\
\hline
SDG 3: Good Health and Well-being &
The system detects contamination and packaging defects that may affect food safety. By ensuring only safe and compliant products reach consumers, it contributes to improved public health and reduces risks associated with unsafe food products. &
Medium (2) \\
\hline
\end{tabularx}
\end{table}

\section{Closing Summary}
\label{sec:closing-summary}

\shorttitle\ provides a foundation for practical AI-based quality inspection in food and beverage manufacturing. The system shows how detection, measurement, severity scoring, remediation, traceability, and user interface design can work together as one workflow. With further dataset expansion, production benchmarking, comparative evaluation, and deployment optimisation, the project can evolve from a capstone prototype into a stronger smart-factory inspection platform.

\clearpage

\chapter*{Appendices}
\addcontentsline{toc}{chapter}{Appendices}
\label{app:appendices}

\section{Dashboard Screenshots}
\label{app:dashboard-screenshots}

This section includes representative screenshots from the operator-facing dashboard. These figures document the working frontend views used for real-time monitoring and manual model inspection.

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.76\textheight,keepaspectratio]{Homepage.png}
\caption{VisionFood QAI dashboard home page}
\label{fig:appendix-homepage}
\end{figure}

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.76\textheight,keepaspectratio]{Manual inspection1.png}
\caption{Manual inspection interface with uploaded product image and inference controls}
\label{fig:appendix-manual-inspection-one}
\end{figure}

\clearpage
\begin{figure}[p]
\centering
\includegraphics[width=\textwidth,height=0.76\textheight,keepaspectratio]{Manual inspection 2.png}
\caption{Manual inspection result view showing annotated output and inference details}
\label{fig:appendix-manual-inspection-two}
\end{figure}

\clearpage

\section{Code Samples}
\label{app:code-samples}

This section records representative code fragments from the inference layer used in the implementation. The first sample is taken from \texttt{inference/bottle\_cap\_v3/detect\_v3.py} and shows the bottle-guided crop used to focus cap detection on the top region of the bottle.

\begin{lstlisting}[language=Python, caption={Cap-region cropping and zooming for cap detection}]
def _get_cap_crop_region(self, bottle_bbox, img_h, img_w):
    """Get the top region of a bottle where the cap lives."""
    x1, y1, x2, y2 = bottle_bbox
    bw, bh = x2 - x1, y2 - y1

    cap_h = int(bh * self.cfg.cap_region_ratio)
    pad_w = int(bw * self.cfg.crop_pad)
    pad_h = int(cap_h * self.cfg.crop_pad)

    cx1 = max(0, x1 - pad_w)
    cy1 = max(0, y1 - pad_h)
    cx2 = min(img_w, x2 + pad_w)
    cy2 = min(img_h, y1 + cap_h + pad_h)
    return cx1, cy1, cx2, cy2

def _zoom_crop(self, crop: np.ndarray) -> np.ndarray:
    """Upscale crop to improve small-object detection."""
    h, w = crop.shape[:2]
    scale = self.cfg.zoom_scale
    new_w, new_h = int(w * scale), int(h * scale)

    max_px = self.cfg.max_zoom_px
    if max(new_w, new_h) > max_px:
        ratio = max_px / max(new_w, new_h)
        new_w, new_h = int(new_w * ratio), int(new_h * ratio)

    if new_w < self.cfg.min_crop_px or new_h < self.cfg.min_crop_px:
        return None
    return cv2.resize(crop, (new_w, new_h), interpolation=cv2.INTER_LINEAR)
\end{lstlisting}

\begin{lstlisting}[language=Python, caption={Applying the cap crop during the second detection pass}]
cx1, cy1, cx2, cy2 = self._get_cap_crop_region(
    bottle["bbox"], h, w
)
crop = img[cy1:cy2, cx1:cx2]
if crop.size == 0:
    continue

zoomed = self._zoom_crop(crop)
if zoomed is None:
    continue

r2 = self.model.predict(
    zoomed,
    conf=cfg.cap_conf,
    iou=cfg.iou_thresh,
    device=cfg.device,
    verbose=False
)
\end{lstlisting}

The fill-level calculation is taken from \texttt{inference/yoloWithFillLevel.py}. It trims the bottle bounding box, clamps the detected water-surface location to the valid bottle area, and converts the vertical distance from the corrected bottle bottom to the water surface into a normalized fill ratio.

\[
\text{fill ratio} =
\frac{y_{\text{bottom,corr}} - y_{\text{water}}}
{y_{\text{bottom,corr}} - y_{\text{top,corr}}}
\]

\begin{lstlisting}[language=Python, caption={Mathematical fill-ratio computation and verdict}]
def compute_fill_ratio(bottle_bbox: list[int],
                       water_bbox: list[int]) -> float:
    bx1, by1, bx2, by2 = bottle_bbox
    bottle_h = by2 - by1

    by1_corr = by1 + int(bottle_h * BOTTLE_TOP_TRIM)
    by2_corr = by2 - int(bottle_h * BOTTLE_BOT_TRIM)
    bottle_h_corr = max(by2_corr - by1_corr, 1)

    water_y = (water_bbox[1] + water_bbox[3]) // 2
    water_y = max(by1_corr, min(by2_corr, water_y))

    fill_from_bottom = by2_corr - water_y
    fill_ratio = fill_from_bottom / bottle_h_corr
    return round(fill_ratio, 3)

def fill_verdict(fill_ratio: float,
                 underfill_thresh: float,
                 overfill_thresh: float) -> str:
    if fill_ratio < underfill_thresh:
        return "underfill"
    elif fill_ratio > overfill_thresh:
        return "overfill"
    else:
        return "normal"
\end{lstlisting}

\begin{lstlisting}[language=Python, caption={Selecting the water-surface box for the detected bottle}]
best_water = None
best_water_conf = 0.0

for box in r_fill[0].boxes:
    wx1, wy1, wx2, wy2 = [int(v) for v in box.xyxy[0].tolist()]
    water_cx = (wx1 + wx2) / 2

    if bx1 <= water_cx <= bx2 and by1 <= wy1 <= by2:
        conf = float(box.conf[0])
        if conf > best_water_conf:
            best_water_conf = conf
            best_water = [wx1, wy1, wx2, wy2]

if best_water:
    fill_ratio = compute_fill_ratio(bottle["bbox"], best_water)
    fill_level = fill_verdict(
        fill_ratio,
        underfill_thresh,
        overfill_thresh
    )
\end{lstlisting}

\section{Detailed Matrices}
\label{app:detailed-matrices}

\begin{table}[!ht]
\centering
\caption{Implementation Traceability Matrix}
\label{tab:implementation-traceability}
\begin{tabularx}{\textwidth}{|P{3.3cm}|P{4.5cm}|Y|}
\hline
\textbf{Implementation Area} & \textbf{Source File} & \textbf{Traceable Purpose} \\
\hline
Cap-region crop and zoom & \footnotesize\path{inference/bottle_cap_v3/detect_v3.py} & Uses the detected bottle box to crop the upper bottle region, enlarge the crop, and improve cap detection for small cap objects. \\
\hline
Fill-level computation & \footnotesize\path{inference/yoloWithFillLevel.py} & Converts bottle and water-surface detections into a normalized fill ratio and classifies underfill, normal fill, or overfill. \\
\hline
Inspection orchestration & \footnotesize\path{inference/yoloWithFillLevel.py} & Combines bottle, cap, label, and fill-level detections into one inspection decision for each product instance. \\
\hline
User interface workflow & Dashboard implementation files & Presents inspection outcomes, summaries, and review-oriented controls discussed in Chapter 4. \\
\hline
\end{tabularx}
\end{table}

\begin{table}[!ht]
\centering
\caption{Cap Detection Crop Matrix}
\label{tab:cap-crop-matrix}
\begin{tabularx}{\textwidth}{|P{3.2cm}|P{3.2cm}|Y|}
\hline
\textbf{Parameter} & \textbf{Role} & \textbf{Effect on Inspection} \\
\hline
\footnotesize\path{cap_region_ratio} & Selects the top portion of the bottle bounding box. & Keeps the second-pass detector focused on the expected cap location. \\
\hline
\footnotesize\path{crop_pad} & Adds horizontal and vertical context around the cap crop. & Reduces missed caps caused by slight bottle-box or cap-position variation. \\
\hline
\footnotesize\path{zoom_scale} & Enlarges the crop before the second detection pass. & Improves visibility of small caps in the detector input. \\
\hline
\footnotesize\path{max_zoom_px} and \path{min_crop_px} & Bound the resized crop dimensions. & Prevents extremely large crops and rejects crops too small for reliable detection. \\
\hline
\end{tabularx}
\end{table}

\begin{table}[!ht]
\centering
\caption{Fill-Level Decision Matrix}
\label{tab:fill-decision-matrix}
\begin{tabularx}{\textwidth}{|P{4.2cm}|P{3.2cm}|Y|}
\hline
\textbf{Condition} & \textbf{Verdict} & \textbf{Interpretation} \\
\hline
\texttt{fill\_ratio < underfill\_thresh} & Underfill & The detected water surface is too low relative to the corrected bottle height. \\
\hline
\texttt{underfill\_thresh <= fill\_ratio <= overfill\_thresh} & Normal & The product fill level lies inside the accepted operating range. \\
\hline
\texttt{fill\_ratio > overfill\_thresh} & Overfill & The detected water surface is too high relative to the corrected bottle height. \\
\hline
No valid water-surface box & Review required & The fill model did not provide a water-surface box that could be associated with the bottle. \\
\hline
\end{tabularx}
\end{table}

% =====================================================================
% References
% =====================================================================
\addcontentsline{toc}{chapter}{References}
\printbibliography[title={References}]

\end{document}
